# Plan · 009 Galería de adopción

1. Copiar fotos a `public/img-cat/`.
2. Modelo `KittenProfile` + `KittensService`.
3. Página ADOPTAR: grid + perfil + adopción con lógica de slots de la 008.
4. Rutas/nav-rail: `adoptar` reemplaza `ai-generator`; hint del dashboard.
5. Baja del generador IA (componente, modelo GeneratedCat, métodos AiService).
6. npm test + build + smoke.
7. Docs: changes.md, roadmap, tasks.
